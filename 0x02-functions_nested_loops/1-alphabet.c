#include "main.h"

/**
 * main - Prints the alphabet in lowercase,
 *
 * Return: Always 0
 *
 */

int main(void)
{
	print_alphabet(alph = 'a'; alph <= 'z';)

	putchar(alph);
	alph++;

	_putchar('\n');

	return (0);
}
