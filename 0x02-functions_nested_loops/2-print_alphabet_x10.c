#include <stdio.h>
/**
 * main - check the code.
 *
 * Return: Always 0.
 *
*/
int main(void)
{
	int alpha = 'a', count;

	while (alpha <= 'z')
	{
		count = 0;
		while (count <= 9)
	{
		printf("alpha");
		count++;
	}
	putchar("\n");
	}
	return (0);
}
