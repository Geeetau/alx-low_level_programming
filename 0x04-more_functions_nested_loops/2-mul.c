#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first character
 * @b: second character
 * Return: multiple of a and b
 */
int mul(int a, int b)
{
	return (a * b)}
}
