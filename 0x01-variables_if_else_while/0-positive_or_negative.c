#include <stdio.h>
#include <time.h>

/**
 * main - assing a random number
 *
 * Return: 0 (Success)
 *
*/
int main(void)
{
	int n;

	if (n > 0)
	{
		printf("%d is positive\n", n);
	}
	else if (n < 0)
	{
		printf("%d is negative\n", n);
	}
	else
	{
		printf("%d is zero\n", n);
	}
	return (0)
